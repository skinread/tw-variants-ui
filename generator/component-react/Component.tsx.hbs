export interface {{pascalCase name}}Props {
  /**
   * Root element ref (React 19+ `ref` as a normal prop)
   */
  ref?: React.Ref<HTMLDivElement>;
  /**
   * Define your props here
   */
  exampleProp?: string;
}

/**
 * describe the {{pascalCase name}} component here
 */
export const {{pascalCase name}} = (props: {{pascalCase name}}Props) => {
  const { ref, exampleProp, ...restProps } = props;

  return <div ref={ref} {...restProps}>{{pascalCase name}} says {exampleProp}</div>;
};
